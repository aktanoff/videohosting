export const HOST = "diploma-testdomain.ddns.net"; // localhost:8000

export const URL = `/api`;
export const WSURL = `ws://${HOST}`;
export const unauthorizedPages = new Set(["/", "/google-auth-failed"]);
